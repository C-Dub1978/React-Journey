/**
 * The input will take its 'type' from props so that it can be either a text field, number, textarea, etc
 */
export default function Input({ inputData }: any): JSX.Element {
    return (<li>1</li>);
}